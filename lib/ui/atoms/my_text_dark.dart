import 'package:flutter/material.dart';

class MyTextDark extends StatelessWidget {
  const MyTextDark(
      {super.key,
      required this.size,
      required this.text,
      required this.color,
      required this.style});
  final double size;
  final String text;
  final Color color;
  final TextStyle style;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
